# Time & locale
APP_TIMEZONE=UTC

# Data paths (kept inside the repo for MVP)
DATA_DIR=/app/data
MODELS_DIR=/app/models

# Open-Meteo (no key needed for non-commercial; read their license)
WEATHER_PROVIDER=open-meteo

# Tariff config
TARIFF_FILE=/app/data/raw/tariffs.csv

# Forecast defaults
FORECAST_HORIZON_HOURS=48
